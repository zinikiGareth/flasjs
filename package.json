{
  "name": "flasjs",
  "type": "module",
  "version": "1.0.0",
  "description": "FLAS JS client library",
  "scripts": {
    "test": "mocha src/test/unit/*.spec.js",
    "package": "node scripts/build.mjs",
    "coverage": "nyc --reporter=html npm run test"
  },
  "devDependencies": {
    "chai": "^5.1.0",
    "esbuild": "0.20.1",
    "mocha": "^10.3.0",
    "nyc": "^14.1.1"
  }
}
